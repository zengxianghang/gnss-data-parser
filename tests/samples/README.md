# Parser test samples

Store only small synthetic, public, or sanitized snippets needed to verify parser behavior.

Do not commit full operational logs or proprietary data. Prefer one or a few representative lines per case and describe the expected behavior in the corresponding unit test.
