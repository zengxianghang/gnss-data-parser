# C/C++ parsers

C/C++ implementations can be added here for native tools or embedded/offline workflows.

Requirements:

- keep output semantics and units aligned with `docs/parser_interface.md`;
- prefer streaming processing for large logs;
- separate parsing from analysis and plotting;
- add small regression samples/tests for parser fixes;
- document intentional differences caused by embedded/runtime constraints.
